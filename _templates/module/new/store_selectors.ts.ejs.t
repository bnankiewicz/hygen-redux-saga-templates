---
to: src/modules/<%= name.toLowerCase() %>/selectors.ts
---
import { createSelector } from "reselect";
import { RootState } from "store/reducers";

// prettier-ignore
const selectors = {
}

export default selectors
